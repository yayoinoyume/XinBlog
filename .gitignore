# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 构建产物（自定义编译脚本产生）
build.err
build.log
compile.done
compile.log

# 运行时数据库（db/ 下为表结构 .sql，运行时生成的库文件应忽略）
*.db
*.sqlite
*.sqlite3

# 环境变量（保留 .env.example 作为模板）
.env
.env.*
!.env.example

# 依赖缓存 / 测试覆盖率
.cache
coverage

# 本地备份目录（.bak 时间戳快照，不入库）
.backups

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 本地开发工具（wrangler 模拟器，不入库）
.dev.vars
.wrangler/
wrangler.toml

# 优化方案文档（本地 .bak 方式管理，不入库）
docs/OPTIMIZATION_PLAN.md
docs/FIX_PLAN.md
